kind = "web"
previewPath = "/"
title = "Infinity AI Voice Assistant"
version = "1.0.0"
id = "artifacts/infinity"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21662

[services.development]
run = "pnpm --filter @workspace/infinity run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/infinity", "run", "build" ]
serve = "static"
publicDir = "artifacts/infinity/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21662"
BASE_PATH = "/"
